const openModalButtons=document.querySelectorAll("[data-modal-target]"),closeModalButtons=document.querySelectorAll("[data-close-button]"),overlay=document.getElementById("noverlay");function openModal(modal){null!=modal&&(modal.classList.add("active"),overlay.classList.add("active"))}function closeModal(modal){null!=modal&&(modal.classList.remove("active"),overlay.classList.remove("active"))}console.log("overlay :"+overlay),openModalButtons.forEach(button=>{button.addEventListener("click",()=>{const modal=document.querySelector(button.dataset.modalTarget);openModal(modal)})}),overlay.addEventListener("click",()=>{console.log("overlay click :"+overlay);const modals=document.querySelectorAll(".nmodal.active");modals.forEach(modal=>{closeModal(modal)});const modals2=document.querySelectorAll(".nmodal2.active");modals2.forEach(modal=>{closeModal(modal)})}),closeModalButtons.forEach(button=>{button.addEventListener("click",()=>{const modal=button.closest(".nmodal");closeModal(modal)}),button.addEventListener("click",()=>{const modal=button.closest(".nmodal2");closeModal(modal)})});